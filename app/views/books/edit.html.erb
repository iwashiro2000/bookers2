<% if @book.errors.any? %>
		<%= @book.errors.count %>
		<p>errors prohibited this obj form being saved:</p>

		<% @book.errors.full_messages.each do |message| %>
		  <%= message %>
		<% end %>
		<% end %>

<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	<h2>Title</h2>
	<%= f.text_field :title %>
	<h2>Opinion</h2>
	<%= f.text_area :body %>

	<h2><%= f.submit 'Update Book' %></h2>
<% end %>

<%= link_to"Show",book_path(@book.id)%>
<span>|</span>
<%= link_to"Back",books_path%>